import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.ssl.pinning',
  appName: 'ionic-ssl-pinning',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
